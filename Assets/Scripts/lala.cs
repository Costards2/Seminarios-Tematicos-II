using UnityEngine;

public class lala : MonoBehaviour
{
    // Start is called once before the first execution of Update after the MonoBehaviour is created
    void Start()
    {
    Screen.SetResolution(1640, 2000, false); 
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
